<%= form_for(@fyoption) do |f| %>
  <% if @fyoption.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fyoption.errors.count, "error") %> prohibited this fyoption from being saved:</h2>

      <ul>
      <% @fyoption.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
